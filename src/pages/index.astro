---
import Layout from "../layouts/Layout.astro";
import profileImage from '../images/yo.jpg';
---

<Layout>
  <main class="font-display">
    <div class="min-h-screen w-full">
      <!-- HEADER -->
      <header
        class="flex items-center justify-between border-b border-primary/40 px-6 py-4 backdrop-blur-md bg-darkPanel/40 shadow-neon"
      >
        <div class="flex items-center gap-4">
          <div class="text-primary size-7 drop-shadow-neon">
            <span class="material-symbols-outlined text-4xl">
              radio_button_checked
            </span>
          </div>
          <h2 class="text-xl font-bold tracking-wide drop-shadow-neon">
            Portafolio de Evidencias
          </h2>
        </div>

        <nav>
          <a href={`${import.meta.env.BASE_URL}/`}>Inicio</a>
          <a href={`${import.meta.env.BASE_URL}/habilidades/`}>Habilidades</a>
          <a href={`${import.meta.env.BASE_URL}/proyectos/`}>Proyectos</a>
          <a href={`${import.meta.env.BASE_URL}/contact/`}>Contacto</a>
        </nav>

        <button class="md:hidden text-gray-100">
          <span class="material-symbols-outlined">menu</span>
        </button>
      </header>

      <section
        id="inicio"
        class="mt-16 mx-6 flex flex-col items-center justify-center gap-6 p-12 rounded-xl text-center border border-primary/40 bg-darkPanel/60 shadow-neon"
        style='background-image: linear-gradient(rgba(0,0,0,0.5), rgba(10,0,16,0.9)), url("/images/hero-bg.jpg"); background-size: cover; background-position: center;'
      >
        <h1
          class="text-5xl md:text-6xl font-black text-white drop-shadow-[0_0_15px_#f20df2] glitch"
          data-text="Jesús Martínez Narciso"
        >
          Jesús Martínez Narciso
        </h1>

        <p class="text-gray-400 max-w-3xl leading-relaxed mt-4">
          Hola, soy <strong>Jesús Martínez Narciso</strong>, tengo 23 años y
          estudio
          <strong>Ingeniería en Sistemas Computacionales</strong> en la UES San José
          del Rincón.
          <br /><br />
          Me apasionan los videojuegos, el anime y la creación de contenido creativo.
          Mi nickname es <strong>JessArt2002</strong>.
        </p>

        <img
          src={profileImage.src}
          alt="Jesús Martínez"
          class="w-40 h-40 rounded-full"
        />
      </section>
    </div>
  </main>
</Layout>
