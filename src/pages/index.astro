---
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
---
<Layout>
  <main class="font-display">
    <div class="min-h-screen w-full">
      <header class="flex items-center justify-between border-b border-primary/40 px-6 py-4 backdrop-blur-md bg-darkPanel/40 shadow-neon">
        <div class="flex items-center gap-4">
          <div class="text-primary size-7 drop-shadow-neon">
            <span class="material-symbols-outlined text-4xl">radio_button_checked</span>
          </div>
          <h2 class="text-xl font-bold tracking-wide drop-shadow-neon">Portafolio de Evidencias</h2>
        </div>
        <nav class="hidden md:flex gap-8 text-gray-300">
          <a href="#inicio" class="hover:text-primary transition">Inicio</a>
          <a href="#habilidades" class="hover:text-primary transition">Habilidades</a>
          <a href="#proyectos" class="hover:text-primary transition">Proyectos</a>
          <a href="#contacto" class="hover:text-primary transition">Contacto</a>
        </nav>
        <button class="md:hidden text-gray-100">
          <span class="material-symbols-outlined">menu</span>
        </button>
      </header>

      <section id="inicio" class="mt-10 flex flex-col items-center justify-center gap-6 p-12 rounded-xl text-center border border-primary/40 bg-darkPanel/60 shadow-neon" 
        style='background-image: linear-gradient(rgba(0,0,0,0.5), rgba(10,0,16,0.9)), url("/dark-neon-red-and-teal-40d4u8kcwxp11ijp.jpg"); background-size: cover;'>
        <h1 class="text-6xl font-black text-white drop-shadow-[0_0_15px_#f20df2] glitch" data-text="Jesús Martínez Narciso">Jesús Martínez Narciso</h1>
        <p class="text-gray-400 max-w-3xl text-center leading-relaxed mt-4">
          Hola, soy <strong>Jesús Martínez Narciso</strong>, tengo 23 años y estudio <strong>Ingeniería en Sistemas Computacionales</strong> en la UES San José del Rincón.  
          Me apasionan los videojuegos, el anime y crear contenido creativo. Mi nickname es <strong>JessArt2002</strong>.
        </p>
        <div class="flex gap-4 mt-4 flex-wrap justify-center">
          <a href="#proyectos" class="px-8 py-3 bg-primary text-white font-bold rounded-lg shadow-neon hover:scale-110 transition">Ver Proyectos</a>
          <a href="#contacto" class="px-8 py-3 bg-darkPanel border border-primary/50 text-white font-bold rounded-lg hover:bg-primary/20 hover:scale-110 transition inline-block">Contactar</a>
        </div>
      </section>

      <section id="habilidades" class="habilidades-section relative py-12">
        <canvas id="particlesSkills" aria-hidden="true" class="absolute inset-0 w-full h-full pointer-events-none"></canvas>
        <div class="container relative z-10">
          <h2 class="skills-title glitch text-3xl md:text-4xl font-black mb-10" data-text="Mis Habilidades">Mis Habilidades</h2>
          <div class="skills-grid grid md:grid-cols-3 gap-6">
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Jugar videojuegos</h3>
              <p>Me gustan los videojuegos musicales y de ritmo rápido.</p>
            </article>
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Dibujar</h3>
              <p>Me gusta dibujar, ya sea en color o en blanco y negro.</p>
            </article>
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Escribir rápido</h3>
              <p>Se escribir rápido en el teclado gracias a juegos rítmicos.</p>
            </article>
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Ilustración digital</h3>
              <p>Uso varios programas para realizar arte digital profesional.</p>
            </article>
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Diseño</h3>
              <p>Diseño creativamente según el proyecto o necesidad.</p>
            </article>
            <article class="skill-card p-6 rounded-lg bg-darkPanel/80 border border-primary/30 shadow-neon hover:scale-105 transition-transform">
              <div class="skill-icon mb-4 text-primary text-4xl"></div>
              <h3 class="text-xl font-semibold mb-2">Ver anime</h3>
              <p>A veces veo anime completo en una sola noche.</p>
            </article>
          </div>
        </div>
      </section>

      <section id="proyectos" class="container mx-auto px-6 py-20">
        <h2 class="text-4xl font-black glitch mb-12 text-center" data-text="Mis Proyectos">Mis Proyectos</h2>
        <div class="grid md:grid-cols-3 gap-8">
          <div class="hud-frame bg-darkPanel p-6 rounded-lg shadow-neon hover:scale-105 transition-transform">
            <h3 class="text-xl font-semibold mb-2">Aplicación para ayuda a necesitados</h3>
            <p class="text-gray-400">Desarrollé una app que ayudaba a personas en situaciones de desastres naturales.</p>
          </div>
          <div class="hud-frame bg-darkPanel p-6 rounded-lg shadow-neonBlue hover:scale-105 transition-transform">
            <h3 class="text-xl font-semibold mb-2">Página web de eventos escolares</h3>
            <p class="text-gray-400">Sitio web para mostrar los eventos más importantes dentro de la universidad.</p>
          </div>
          <div class="hud-frame bg-darkPanel p-6 rounded-lg shadow-neon hover:scale-105 transition-transform">
            <h3 class="text-xl font-semibold mb-2">Registro de estudiantes</h3>
            <p class="text-gray-400">Sitio para registrar a los estudiantes que forman parte de la institución.</p>
          </div>
        </div>
      </section>

      <section id="contacto" class="mt-16 max-w-xl mx-auto hud-frame relative p-10 rounded-xl bg-darkPanel/80 border border-primary/30 shadow-neon">
        <div class="hud-scan"></div>
        <h2 class="text-3xl font-bold text-center mb-6 drop-shadow-neon">Envíame un mensaje</h2>
        <form class="flex flex-col gap-4">
          <input type="text" placeholder="Tu nombre" class="bg-darkPanel/60 border border-primary/40 p-3 rounded-lg text-white focus:ring-primary focus:outline-none" />
          <input type="email" placeholder="Correo electrónico" class="bg-darkPanel/60 border border-primary/40 p-3 rounded-lg text-white focus:ring-primary focus:outline-none" />
          <textarea placeholder="Tu mensaje..." rows="5" class="bg-darkPanel/60 border border-primary/40 p-3 rounded-lg text-white focus:ring-primary focus:outline-none"></textarea>
          <button class="px-8 py-3 bg-primary text-white font-bold rounded-lg shadow-neon hover:scale-110 transition" type="submit">Enviar mensaje</button>
        </form>
      </section>

      <footer class="mt-20 border-t border-primary/40 py-10 text-center">
        <p class="text-gray-500 text-sm mt-4">© 2025 JessArt2002. Todos los derechos reservados.</p>
      </footer>
    </div>
  </main>

  <script type="module">
    (function () {
      const canvas = document.getElementById("particlesSkills");
      const section = document.querySelector(".habilidades-section");
      if (!canvas || !section) return;
      const ctx = canvas.getContext("2d");
      if (!ctx) return;

      function setSize() {
        const rect = section.getBoundingClientRect();
        const dpr = window.devicePixelRatio || 1;
        canvas.width = Math.floor(rect.width * dpr);
        canvas.height = Math.floor(rect.height * dpr);
        canvas.style.width = rect.width + "px";
        canvas.style.height = rect.height + "px";
        ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      }
      setSize();
      window.addEventListener("resize", setSize);

      const PARTICLE_COUNT = Math.min(180, Math.max(50, Math.floor((canvas.width / 1000) * 120)));
      const particles = [];
      for (let i = 0; i < PARTICLE_COUNT; i++) {
        particles.push({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, size: Math.random() * 3 + 0.8, speedX: Math.random() * 0.8 - 0.4, speedY: Math.random() * 0.8 - 0.4, hueShift: Math.random() * 40 - 20 });
      }

      function animate() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        const g = ctx.createLinearGradient(0, 0, canvas.width, canvas.height);
        g.addColorStop(0, "rgba(125,0,255,0.02)");
        g.addColorStop(1, "rgba(0,0,0,0.02)");
        ctx.fillStyle = g;
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        particles.forEach(p => {
          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
          ctx.fillStyle = `hsl(${285 + p.hueShift} 100% 60%)`;
          ctx.shadowBlur = 18;
          ctx.shadowColor = "rgba(255,0,255,0.9)";
          ctx.fill();
          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size * 3.2, 0, Math.PI * 2);
          ctx.fillStyle = "rgba(160,0,255,0.06)";
          ctx.fill();
          p.x += p.speedX; p.y += p.speedY;
          const w = canvas.width; const h = canvas.height;
          if (p.x < -10) p.x = w + 10;
          if (p.x > w + 10) p.x = -10;
          if (p.y < -10) p.y = h + 10;
          if (p.y > h + 10) p.y = -10;
        });
        requestAnimationFrame(animate);
      }
      animate();
    })();
  </script>
</Layout>
